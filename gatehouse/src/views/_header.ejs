<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title><%= typeof title !== "undefined" ? title : "Gatehouse" %></title>
    <link rel="stylesheet" href="/styles.css" />
    <% if (typeof extraHead !== "undefined") { %><%- extraHead %><% } %>
</head>
<body>
<header class="topbar">
    <a class="brand" href="/login">Gatehouse</a>
</header>
<main>
